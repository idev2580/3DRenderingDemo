# 3D Rendering Demo

Rendering Demo for 3DML

## How to run

1. Add `/assets` directory and put `nub1.glb` and `splat.ply` in that directory.
2. Open Webserver and connect through a web browser.(Must be hosted by web server!!!)